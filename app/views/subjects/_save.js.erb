$("ul.errors").html("")
<% if @subject.errors.any? %>
  <% @subject.errors.full_messages.each do |message| %>
    $("ul.errors").append($("<li />").html("<%= message.html_safe %>"))
  <% end %>
<% else %>

  $("#modals").modal("hide");
    location = "<%= subjects_path %>";
<% end %>